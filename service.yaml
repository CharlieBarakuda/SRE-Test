# Делает масштабирование прозрачным и поды за сервисом балансируются автоматически.
apiVersion: v1
kind: Service
metadata:
  name: app-svc
  namespace: webapp-prod
spec:
  selector:
    app: app
  ports:
    - port: 80
      targetPort: 80
